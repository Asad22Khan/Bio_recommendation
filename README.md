### Trained Model is already stored in pickle file which is available in the github repository
### To run the API you first have to open "main.py" file and run the file with the following command 
### "python -m uvicorn main:app --reload"
